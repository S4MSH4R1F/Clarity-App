import { View, Text } from "react-native"; 

export default function MppExplainer() { 
    
    return ( <View className="flex-1 items-center justify-center bg-white"> 
    <Text className="text-lg font-semibold">What does an MPP do?</Text> 
    </View> 
    ); 
} 
